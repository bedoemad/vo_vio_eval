# Configuration files

This folder contains benchmark configuration files.

## Local paths

Copy:

```bash
cp configs/local_paths.example.json configs/local_paths.json


## Check that the cleanup worked

Run:

```bash
python -m compileall src adapters